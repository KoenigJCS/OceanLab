using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEditor.Rendering.HighDefinition;
using UnityEngine;

public class PFieldVis : MonoBehaviour
{
   //spublic Material pFieldMat;
    // Start is called before the first frame update
    void Start()
    {
        //pFieldMat.SetFloat("eRep",AIMgr.inst.eAvoidance);
    }

    // Update is called once per frame
    void Update()
    {
        //pFieldMat.SetVector("NetPField",GetComponent<BoatEntity>().repelPotential);
    }
}
